# ============================================
# Step 1: শুধু basic server চালানোর জন্য দরকারি package
# পরের ধাপগুলোতে (AI pipeline, database, auth, payment)
# আরও package এখানে যোগ হবে।
# ============================================

fastapi==0.115.0
uvicorn[standard]==0.30.6

# Step 2: ছবি/ফাইল আপলোড হ্যান্ডেল করার জন্য এটা দরকার
python-multipart==0.0.12

# Step 3: AI Pipeline - ছবি প্রসেসিং ও SVG বানানোর জন্য
# (তোমার Python ভার্সন নতুন বলে version pin না দিয়ে সর্বশেষ compatible ভার্সন নিচ্ছি)
opencv-python-headless>=4.10.0
numpy>=1.26.0
svgwrite>=1.4.3

# Step 10: Login/Register - ডাটাবেজ, পাসওয়ার্ড হ্যাশিং, JWT টোকেন
sqlalchemy>=2.0.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pyjwt>=2.8.0
pydantic[email]>=2.0.0

# Step 11: PDF/EPS Export - SVG কে অন্য ফরম্যাটে রূপান্তর করার জন্য
# (এগুলো pure Python প্যাকেজ, Windows-এ Cairo/Inkscape এর মতো আলাদা
# কোনো সিস্টেম প্রোগ্রাম ইনস্টল করা লাগে না)
svglib>=1.5.1
reportlab>=4.0.0

# Step 11: DXF Export - CNC/CAD/লেজার-কাটিং সফটওয়্যারের জন্য ভেক্টর ফরম্যাট
ezdxf>=1.3.0

# Step 12: Security - Rate limiting + শক্ত image validation
slowapi>=0.1.9
Pillow>=10.0.0
